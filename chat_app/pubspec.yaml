name: micommunity
description: "Xiaomi Community."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Novas dependências para o chat
  http: 0.13.6
  provider: ^6.0.7
  flutter_secure_storage: ^8.1.0
  uuid: ^4.5.2
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.5
  signalr_core: ^1.1.0
  signalr_netcore: ^1.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

flutter_launcher_icons:
  android: true
  ios: true

